// Service Removed.
// This application is now fully static and does not require external AI APIs.
// All tips are served locally from the Tips page component.

export const getSmartGardeningTip = async (query: string): Promise<string> => {
  return "";
};

export const generateMoreTips = async (category: string): Promise<any[]> => {
  return [];
};